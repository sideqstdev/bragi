overwrite: true
schema: "http://localhost:8080"
generates:
  src/lib/codegen.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
  ./graphql.schema.json:
    plugins:
      - "introspection"
